## 3.无重复字符的最长子串
Target:1.子串；2.长度最长
key point：观察可重复用到的信息；
滑动窗口法：使用left左边界和right右边界来进行滑动处理
Tip：处理字符串时可以使用unordered_set!
时间复杂度的分析：只有当内层循环对外层的每一次循环都可能重新完整执行 n 次时，才通常是 O(n²)
#* Key point：右指针负责扩大窗口；出现重复时，左指针负责缩小窗口，直到窗口重新满足“没有重复字符” *#

## 438.找到字符串中的所有字母异位词
本题的滑动窗口可以直接固定长度，即窗口的长度不用实时变化
比较点：
1.是否是相同的字符
2.相同字符出现的次数是否一致
key point:实际上就是先固定要找的字符串的窗口，然后再去待比较串里面比对寻找
Tip：注意不同窗口的数据结构的构造