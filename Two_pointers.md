## 283.移动零
使用快慢指针slow和fast；slow指向零元素，fast指向非零元素时进行交换，当fast最终指向零元素时则结束
key point：
当fast遇到0时继续向后移动；
当fast遇到非零元素时：
1.fast所指元素和slow所指元素进行交换；
2.fast指针和slow指针同时向后移动
Tip：slow指针的移动一定要放在交换元素位置的逻辑内，不然会失去slow的交换位置

## 11.盛最多水的容器
Tip：本题使用的双指针一个从左边出发，另一个从右边出发
key point：一开始时让容器有最大的宽度，即left & right
移动指针时移动较短的那一条木板