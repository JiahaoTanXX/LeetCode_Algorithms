## 53.最大子数组和
方法：动态规划
找到当前最大的currentSum，并在此基础上比对获得maxSum

## 56.合并区间
先按左端点的大小对所有区间进行排序
key point：
1.对所有数组进行排序
2.比较前后两个数组的左右端点，完成合并或插入工作
key point：先排序，再比较当前区间左端点与结果末尾区间的右端点

## 189.轮转数组
Tip:思考如何减少重复的遍历和判断
方法：三次反转法
Step:
1.翻转整个数组
2.翻转前k个元素
3.翻转后n - k个元素

## 238.除了自身以外的数组乘积
Tip：既然是除了自身以外的乘积，那么我们可以以自身元素的位置为分界，求左边元素的乘积和和右边元素的乘积和 
key point:
1. 从左往右求左边数组乘积和
2. 从右往左乘上右边乘积和